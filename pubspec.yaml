name: proyecto_moviles3
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.0.0 <5.0.0'
  flutter: '>=3.0.0 <5.0.0'

dependencies:
  firebase_auth: ^6.4.0
  firebase_core: ^4.7.0
  flutter_native_splash: ^2.4.8
  icons_launcher: ^3.1.0
  flutter:
    sdk: flutter
  image_picker: ^1.2.3
  supabase_flutter: ^2.15.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  video_player: ^2.8.0
  chewie: ^1.7.0

flutter:
  uses-material-design: true
  assets:
  - assets/data/
  - assets/images/